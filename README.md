# Vagrant-Centos-Staging

## init

```
git clone https://github.com/kookaa/Vagrant-Centos-Staging.git
cd Vagrant-Centos-Staging
vagrant up --provision
vagrant ssh host
```

## host to sandbox

```
ssh sandbox
```
